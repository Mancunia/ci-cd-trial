name: Hello_world

on:
  push:
    branches:
      - main
      - dev

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello World
        run: echo "Hello, World!"
      - name: Print Current Date and Time
        run: date
